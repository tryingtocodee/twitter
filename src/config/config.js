const config = {
    development : {
        username : "mubeen_user",
        password : "my-password",
        database : "twitter",
        host : "127.0.0.1",
        dialect : "mysql"
    }
}

module.exports = config